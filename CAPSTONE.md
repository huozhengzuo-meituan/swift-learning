# 毕业项目：NativeStudy 复习计划

## 目标与用户故事

在已有 iOS/macOS 学习管理应用中独立增加一个完整功能：为条目安排下一次复习，找到今天应复习的条目，完成后推进计划。保留用户已有数据，并解释设计边界。

## 必做需求

1. 模型包含可选的下次复习日期与最近完成时间，稳定 ID 不变。未设置日期的旧数据仍可正常展示。
2. 编辑页可安排/清除日期；取消编辑不会写入实体。无效标题继续被拒绝。
3. 列表可查看今日与已逾期的待复习条目；明确“今天”使用哪个 Calendar/timeZone。
4. 点击“完成本次复习”后按 1 / 3 / 7 天中的选择推进日期；保存失败时提示并避免假成功。
5. 关闭、重开后计划仍存在；删除当前选中条目不会留下不可操作详情。
6. iPhone 支持紧凑导航与键盘输入；Mac 支持窗口缩放和键盘操作。新增标签进入 String Catalog，状态不能只靠颜色区分。

## 推荐实现顺序

先写纯日期规则和测试，再设计模型兼容与保存路径，随后加入表单与筛选，最后完成两平台交互。每阶段保留可运行版本。允许参考课程，不直接索要完整毕业代码；需要帮助时优先请求局部提示。

## 验收与评分

| 维度 | 分值 | 通过证据 |
|---|---:|---|
| 规则与类型 | 15 | 无计划、今日、逾期、跨日、不同 Calendar 输入；无需 UI 即可测试 |
| 状态所有权 | 15 | 能画出 App/窗口/页面/草稿的所有者；取消不污染原数据 |
| 持久化与兼容 | 20 | 新上下文/重启读回；旧数据兼容说明与验证；保存失败不误报成功 |
| 并发与恢复 | 15 | Swift 6 检查通过；取消不显示失败；过期异步结果不覆盖新结果 |
| 平台与可访问性 | 15 | iOS/macOS 核心路径，Mac 键盘、iOS大字号、VoiceOver朗读；记录实测系统 |
| 工程证据 | 15 | 测试、构建、操作步骤、日志与限制可复现 |
| 解释与反思 | 5 | 能讲清两个取舍、一个尚存风险以及下一步验证 |

建议门槛 80 分，且不能存在数据丢失、崩溃或编译失败。未有设备时可保留真机项未验，不写成已通过；相应能力评估暂不完成。

## 必答解释题

- 为什么 `let` 修饰 class 引用不保证对象不可变？
- 为什么 `@Observable` 和 `@MainActor` 不是同一职责？
- actor 方法经过 `await` 后为什么可能需要重新检查条件？
- `.task(id:)` 何时重新开始、何时取消？取消是否必然马上停止？
- `@State`、`@Binding`、`@Bindable`、`@Environment` 的真值来源分别在哪里？
- 最低部署版本、SDK、编译器版本与语言模式怎样相互影响？
- 为什么用 Debug 编译通过不能证明 Release 可分发？

## 学习者填写的证据表

| 项目 | 环境与命令/步骤 | 结果 | 限制 |
|---|---|---|---|
| 规则与失败测试 | 待填写 | 未验 | — |
| Swift 6 并发检查 | 待填写 | 未验 | — |
| macOS 核心操作 | 待填写 | 未验 | — |
| iOS 模拟器核心操作 | 待填写 | 未验 | — |
| 旧数据升级 | 待填写 | 未验 | — |
| 真机与最低系统 | 待填写 | 未验 | — |
| VoiceOver / Dynamic Type | 待填写 | 未验 | — |
| 签名与分发 | 自选后续实践 | 未验 | 不属于本次自动发布 |

完成后请求：“请评审我的毕业项目，依据 CAPSTONE.md 逐项检查，并针对未掌握处提问。”仅在展示理解后新增 learning-records。
